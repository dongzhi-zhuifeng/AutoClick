{
  "mcpServers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx.cmd",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ]
    },
    "command": {
      "type": "stdio",
      "command": "npx.cmd",
      "args": [
        "-y",
        "mcp-server-commands"
      ]
    }
  }
}
